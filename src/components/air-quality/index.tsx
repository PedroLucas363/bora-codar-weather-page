import Section from "../section";

import styles from "./index.module.css";

type Props = {};

function AirQuality({}: Props) {
  return <Section className={styles.airQuality}>sunTime</Section>;
}

export default AirQuality;
