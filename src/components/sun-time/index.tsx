import Section from "../section";

import styles from "./index.module.css";

type Props = {};

function SunTime({}: Props) {
  return <Section className={styles.sunTime}>sunTime</Section>;
}

export default SunTime;
