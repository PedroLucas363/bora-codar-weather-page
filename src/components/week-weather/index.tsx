import Section from "../section";

import styles from "./index.module.css";

type Props = {};

function WeekWeather({}: Props) {
  return <Section className={styles.weekWeather}>sunTime</Section>;
}

export default WeekWeather;
